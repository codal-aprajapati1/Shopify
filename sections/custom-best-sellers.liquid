{{ 'custom-best-seller.css' | asset_url}}
<div class="best-seller-container">
  <div class="best-seller-sub-container page-width">
    <div class="Page-headig-container">
      <h2>{{ section.settings.sect_heading }}</h2>
    </div>
    <div class="best-seller-slider">
      <div class="slider-navbar"><h3>{{  }}</h3></div>
      <div class="actual-slider-container">
        <div class="slider-container">
          <div class="product-image-cont">
            <img src="{{   img_url }}" width=100% height=100%>
          </div>
          <div class="info-container">
            <div class="reviews-container">{{  }}</div>
            <div class="title-container">{{  }}</div>
            <div class="description-container">{{  }}</div> 
            <div class="button-container">
              <a href="{{  }}"> Shop now <span class="price-container">{{  }}</span></a>
            </div>
        </div>
      </div>
    </div>
  </div>
</div>

<script>
  
</script>

{% schema %}
  {
    "name": "Best Sellers",
    "tag": "section",
    "settings": [
      {
      "type": "text",
      "label": "Enter the heading of the section",
      "id": "sect_heading"
      }
    ],
    "blocks" : [
      {
       "type": "navlinks",
       "name": "Enter the navlinks",
       "settings":[
         {
           "type" : "text",
           "id": "nav-text",
           "label": "Enter the name of the navlink"
         },
         {
           "type" : "product",
           "id": "product",
           "label": "Select the product"
         }
       ] 
      }
    ]
  }
{% endschema %}